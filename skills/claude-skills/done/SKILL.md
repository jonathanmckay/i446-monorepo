---
name: "done"
description: "Stop current Toggl timer and mark the task done. Alias for /did with no arguments. Usage: /done"
user-invocable: true
---

# Done (/done)

Thin wrapper for `/did` with no arguments. Stops the running Toggl timer and routes the timer description through the /did pipeline.

## Behavior

Invoke `/did` with no arguments. See the `/did` skill for full documentation of no-args mode.
